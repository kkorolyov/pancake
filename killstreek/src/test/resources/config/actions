# Defines new actions composed of known actions
# Each action is mapped to a 3-action tuple defining (press, hold, release) behavior

MOVE_UP=FORCE_UP,WALK,FORCE_DOWN
MOVE_DOWN=FORCE_DOWN,WALK,FORCE_UP
MOVE_LEFT=FORCE_LEFT,WALK,FORCE_RIGHT
MOVE_RIGHT=FORCE_RIGHT,WALK,FORCE_LEFT
