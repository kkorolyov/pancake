name: publish

on:
  push:
    branches:
      - master

jobs:
  publish:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        version: [ 17 ]

    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: publish-gradle
        uses: kkorolyov/publish-gradle@0.3.0
        with:
          java-version: ${{ matrix.version }}
          publish-task: publish allDocs
          token: ${{ secrets.GITHUB_TOKEN }}
